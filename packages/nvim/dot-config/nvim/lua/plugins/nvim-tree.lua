return {
    "nvim-tree/nvim-tree.lua",
    lazy = false,
    opts = {
        filters = {
            dotfiles = false
        },
        update_focused_file = {
            enable = true,
        }
    }
}
